# SCMDB Quest Recipe Patcher Releases

## v2.2.1

- launcher больше не выглядит зависшим во время первого долгого `Пропатчить`;
- добавлена общая полоса прогресса в launcher;
- лог операции теперь обновляется по ходу работы, а не только после завершения патчера;
- на долгой загрузке Wiki-рецептов показывается прогресс `X/Y`;
- release ZIP включает проверенный Wiki cache, чтобы свежая установка не тратила несколько минут на первичную загрузку всех рецептов;
- исправлено аварийное закрытие launcher после нажатия `Пропатчить`: вывод дочернего PowerShell теперь читается через `reports\launcher-run-*.log` с UI-потока.

## v2.2.0

- метка чертежей в списке контрактов сокращена с `[ЧЕРТЁЖ]` до `[Ч]`;
- добавлена метка `<EM4>[А]</EM4>` для контрактов с опциональным пилотом-асом (`AcePilot`);
- добавлена метка `[С]` для контрактов с обменной scrip/coin-наградой, включая `MG Scrip` и `Council Scrip`;
- порядок меток в названии контракта: `[Ч] <EM4>[А]</EM4> [С]`;
- launcher показывает статистику новых меток в сводке отчёта.

## v2.1.1

- кнопка launcher `Проверить` заменена на быструю `Проверить путь`;
- быстрая проверка больше не загружает SCMDB и Star Citizen Wiki API, поэтому не выглядит как зависание;
- полный dry-run оставлен как консольная команда с `-DryRun` и описан в README;
- основной патч, backup, rollback, отчёты и игровые подсказки не менялись.

## v2.1.0

- для рецептов подтягиваются ингредиенты из Star Citizen Wiki API;
- описания планет и лун получают блок `Крафт-подсказка (SCMDB)`;
- планетные подсказки показывают рецепты по категориям, подразделам и только те ресурсы, которые добываются на выбранной локации;
- добавлена легенда прямо на планетах: `[К] корабль`, `[Т] наземная техника`, `[М] мультитул`;
- добавлены фильтры планетных подсказок: корабельные компоненты только `Grade A Military/Stealth`, FPS-оружие без пистолетов и дробовиков, расходники скрыты;
- однотипные рецепты на планетах схлопываются в серии/сеты: `Artimex set`, `Attrition Repeaters 1-6`, `Lightstrike Cannons I-VI`, `Deadbolt Cannons I-VI`;
- квестовые блоки теперь используют тот же порядок категорий и подразделы для компонентов, корабельных орудий, брони и FPS-оружия;
- формат ресурсов: `[К] корабль`, `[Т] наземная техника`, `[М] мультитул`;
- цветовая разметка приведена к рабочему варианту Star Citizen UI: `EM4` для верхних категорий, подразделов и `[К]`;
- журнал `Основы добычи ресурсов #2` больше не заменяется, потому что без интерактивного меню полный справочник неудобен в Mobiglas;
- классифицированы оставшиеся `Fuel Nozzle`-рецепты через local overrides; `unknownBlueprints` снова `0`;
- добавлен отдельный cache для Wiki blueprint API;
- отчёты и launcher показывают статистику по планетным подсказкам.

## v2.0.0

- добавлен Windows launcher;
- добавлен выбор папки `StarCitizen\LIVE` через стандартное окно Windows;
- добавлены кнопки `Проверить`, `Пропатчить`, `Откатить backup`, `Открыть отчёты`;
- добавлено обогащение рецептов через Star Citizen Wiki API;
- добавлен локальный cache Wiki API;
- добавлен `data/blueprint-overrides.ru.json`;
- рецепты группируются по типам;
- `Metamaterial Test #146` вынесен из неизвестных в группу `Материалы/особое`;
- отчёты содержат `unknownBlueprints`;
- добавлен скрипт сборки ZIP-архива;
- исправлен запуск кнопок `Проверить`, `Пропатчить` и `Откатить backup` из launcher;
- добавлена короткая сводка результата в окне launcher;
- проверено в игре автором.

## v1.0.0

- добавлена метка `[ЧЕРТЁЖ]` в список контрактов;
- добавлен список рецептов SCMDB в описание контрактов;
- добавлены backup и JSON-отчёты;
- подтверждён успешный тест у 3 игроков.
